stages:
  - build
  - deploy

build website:
  image: node:16.13-alpine
  stage: build
  script:
    - yarn install
    - yarn build
  artifacts:
    paths:
      - build  

deploy to s3:
  stage: deploy
  image: 
    name: amazon/aws-cli:2.11.3
    entrypoint: ['']
  script:
    - aws --version
    - aws s3 sync build s3://$AWS_S3_BUCKET --delete
